# Plan 1.2 — Execution Summary

**Status:** COMPLETE
**Commit:** 7e461db

## Changes Applied

- **narratorAgent.ts:**
  - Assinatura de `generateAllNarrations` alterada: retorno mudou de `Promise<VideoChapter[]>` para `Promise<{ chapters: VideoChapter[]; failedScenes: number }>`
  - Variável `failedCount` adicionada antes do loop principal
  - Bloco `catch` reescrito: `console.error` substituído por `console.warn` com preview do texto (50 chars); duração calculada por `Math.ceil(text.trim().split(/\s+/).length / 2.5)` ao invés de `5` hardcoded; `durationSec` = `estimatedDuration + 0.3`; `failedCount++` incrementado em cada falha
  - `return result` substituído por `return { chapters: result, failedScenes: failedCount }`

- **pipelineOrchestrator.ts:**
  - Desestruturação aplicada na linha de await: `const { chapters: chaptersWithAudio, failedScenes: ttsFailedCount } = await logStep(...)`
  - Bloco de aviso adicionado após o await: se `ttsFailedCount > 0`, emite `console.warn` e `toast.warning` com a contagem de cenas em fallback

## Bugs Fixed

- **BUG-007:** CONFIRMADO CORRIGIDO. O valor `durationSec: 5.3` hardcoded foi removido. Agora o fallback usa o mesmo algoritmo de estimativa de `callTTS` em `llm.ts:249` (`Math.ceil(palavras / 2.5)`), mantendo coerencia com o caminho feliz. Timestamps do SEO nao mais corrompidos quando cenas falham no TTS.

- **BUG-008:** CONFIRMADO CORRIGIDO. O retorno agora inclui `failedScenes: number`. O orchestrator exibe `toast.warning` com a contagem exata de cenas em fallback, permitindo distinguir "pipeline 100% com TTS real" de "pipeline concluido com N cenas em browser_tts".

## Issues Encountered

- **Conflito de merge no `.env`:** O arquivo `.env` estava em estado `UU` (unmerged) com marcadores de conflito entre `<<<<<<< Updated upstream` e `>>>>>>> Stashed changes`. As duas versoes divergiam apenas nas chaves de API (`OPENROUTER_API_KEY`, `AI33_API_KEY`, `VITE_*`). Resolucao: preservadas as "Stashed changes" (versao local mais recente). O `.env` foi incluido no commit de resolucao. Nota para o dev: verificar qual conjunto de chaves e o correto para producao e atualizar manualmente se necessario.

- **TypeScript:** `npx tsc --noEmit` retornou sem erros apos as alteracoes.

- **Sem outros callers:** `generateAllNarrations` e chamada apenas pelo `pipelineOrchestrator` — confirmado pelo risco mapeado no plano. Nenhum outro arquivo precisou de ajuste.
